<div id="users_page">
	<h1>Users</h1>

	<% @users.each do |user| %>
		<% if user.user_name != session[:session_user_name] %>
			<a href="/<%= user.user_name %>">
				<%= user.user_name %>
			</a>
			<br />
		<% end %>
	<% end %>
</div>